## Validation scripts
WIP
The idea is when the program is ran, the config and all scripts are copied into the config_log directory and then all the enviroment varables needed to run the scripts are added to the bash instance.  When the script is ran, all the configration is setup needed to read seial and load tests onto the RISCV core.  the results will be logged.  At the end the results will be outputted into a excel file.
